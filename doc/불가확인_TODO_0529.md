# 불가확인(크롤링) 작업 — 이어서 진행할 때 이 파일 참고

> **재개 트리거**: "크롤링 불가확인 탭 마저 진행해줘" → 이 파일 읽고 [남은 작업](#남은-작업-우선순위) 순서대로.
> 작성일 2026-05-29(금). 작업: 06-01(월) 대규모 개편, 06-02(화) 공매 검증·보강.
> 관련 메모리: `project_buga_pegi_model` (전체 설계/단계표).
> ⚠ **크롤러 파일명 변경**: `03.cc.py` → **`04.cc.py`** (진행사항 확인 통합본). 03.cc.py는 호환 shim만 남음.

---

## 1. 전체 그림 (불가/폐기 물건상태 모델)

- **상태값(stu_member)**: `폐기`(내부 불필요, 빨강) / `불가`(법원 진행불가, 검정). 기존 `취소`·`변경` 제거됨(사용자가 DB에서 전부 `폐기`로 통합 완료).
- **세부 2열 (items 시트 맨끝)**: `Z(26)=stu_reason`(불가사유: 변경/취소/취하/정지/연기/기각), `AA(27)=stu_reason_detail`(상세/종결문구). 기존 열 절대 손대지 말 것 — [[feedback_items_columns_append_only]]. (X=24 chuchen_read, Y=25 chuchen_read_date)
- **키 = (사건번호, 입찰일자, 법원) 3개.** item_id 안 씀(사용자 결정). 옥션은 사건번호로만 검색 → 가져와서 3키 일치 검증.
- **법원 변환**: 옥션 결과 주소 → `00.imageup/court_jurisdiction.py` 의 `find_court` → MAPS 법원명 (정변환). 역변환 안 함.

## 2. 완료된 것 (배포 @1243~@1260)

| 단계 | 내용 | 배포/커밋 |
|---|---|---|
| P1 | items Z/AA열 읽기(readAllData) + saveItemReason/ensureItemReasonColumns_ (SheetDB.js) | @1246 |
| P2 | 상태 enum 정리(드롭다운/필터/회원화면/대시보드 카드·차트/업무현황 표) 취소·변경→폐기·불가 | @1248~@1255 |
| P3 | 물건상세 '불가/폐기 사유' 서브탭(불가사유 드롭다운+상세+저장) + 상단 수정버튼도 저장 | @1256~@1257 |
| P4 | 입찰일정 물건 모달에 동일 '불가/폐기 사유' 탭 | @1257 |
| P5 | 폐기/불가 상태값 hover 풍선(목록·상세·모달·입찰일정 카드) | @1258~@1259 |
| 매니저 | cc 탭 이름 "변경/취소 확인" → **"불가확인"** | 4e4d141 |
| P6 | 불가확인 3키 검증 워크플로 — 아래 3) 참조 | @1260 (bec4c7c) |
| 부가 | 카카오 실행 의존성(pyautogui/pyperclip/pygetwindow) 설치 — 매니저 카카오 실행 복구 | (pip) |

## 3. P6 현재 상태 (@1260 배포됨, 동작 흐름)

1. MAPS **"~7일 리스트 다운"** (입찰물건관리 일괄작업) → `사건번호ǀ입찰일자ǀ법원` 3필드 .txt (js-app `handleDownload7DaysList`)
2. 매니저 **불가확인** 탭 → 그 파일/텍스트 입력 → 실행 → `00.imageup/03.cc.py`
3. 03.cc.py: 사건번호+연도 검색 → 결과 주소→법원변환 → 3키 검증 → 종결문구 사유/상세 파싱 → `RESULT|json`
4. cc 결과표: 불가사유+3키일치만 자동체크 → "불가 처리" 전송 → crawler `/api/maps-changecancel` → GAS `uploadChangeCancel`(3키 매칭 → 불가+사유(Z)+상세(AA))

---

## 2-b. 06-02(화) 추가 처리 (배포 @1270 / 매니저 git)

- **공매 경로 실거동 검증 완료** — cc 활성계정으로 04.cc.py 직접 실행. 진행중(2025-12140-001)·전주낙찰(2025-08032-001) 둘 다 정확 분류(진행/매각), key_match ✅.
- **공매 검색 물건상태=전체 강제** (04.cc.py `set_gongmae_state_all`) — '낙찰' 옵션 가진 select만 물건상태로 식별('매각'은 처분방식과 겹침) 후 전체 선택. 낙찰물건 검색누락 방지. (커밋 d797d93)
- **공매 낙찰가 파싱 수정** (04.cc.py `parse_gongmae_detail`) — 공매 상세는 `낙찰 : 234,888,999원` 형태(경매 '매각:'과 다름). '낙찰 :' 패턴 추가. 매수인은 공매 상세에 미공개라 공란 정상. (커밋 5670196)
- **#4 일부 처리** — 텔레그램 흐름모달 라벨 신모델 정렬(`_MSG_FLOW` seq10 '변경'→'불가', statusMap `item_card.status` '취소'→'불가'). 전송기능 영향 없는 표시라벨. **남은 것**: `sumTotals.취소/변경`(월별집계)은 GAS 백엔드 summary 키와 엮여 보류.
- **#3 처리 완료** — 03.k.py `extract_auction_parts` year_val 빈 사건번호 → num1 '전체연도'(value=0) 강제 fallback (기존 skip→연도필터 누락 방지).
- **#5 결정** — 불가/폐기 사유 탭: **일단 항상 표시(현행 유지)**. 사용자 테스트 완료 후 재안내 예정. (비활성화 보류)

## 남은 작업 (우선순위)

### ✅ #1 (핵심) 불가확인 탭에서 직접 "MAPS 리스트 불러오기" 버튼 — 완료 (@1261, 2026-06-01)
- **GAS** `SheetDB.js`: `get7DaysBugaList()` 신설 — items B/C/D만 읽어 오늘~+7일(YYMMDD 집합, 월/연경계 안전) 필터 → `{sakun_no, bid_date(YYMMDD), court}` 배열. `handleJosaApiPost_` 디스패처에 `get7DaysBugaList` 추가.
- **Main.js** doPost 라우팅 버그 수정: `'Josa' 미포함` 액션(`uploadChangeCancel`/`getInvestigators`/`get7DaysBugaList`)이 search 라우터로 잘못 가던 것 → `JOSA_EXTRA_ACTIONS` 화이트리스트로 josa 라우터 정상 연결. **(P6 전송 경로 차단 버그였음)**
- **mj_tabs.js**: `getMapsAdminKeyMj()`(localStorage `auction1_maps_admin_key`) + `loadMaps7DaysList()` + cc-load7 버튼 바인딩. `sendCcToMaps`에 `api_key` 추가(누락→인증실패 버그 수정).
- **index.html** cc 카드: "📥 MAPS 7일 리스트 불러오기" 버튼(입력 라벨 우측).
- crawler `/api/maps-gas` 프록시는 payload.api_action 그대로 전달 → 변경 없음.
- ⚠ 미검증: 실제 매니저 새로고침 후 버튼 클릭 → MAPS 7일 데이터 로드 + "불가 처리" 전송 end-to-end (사용자 테스트 필요).

### #2 04.cc.py 경매 불가건 DOM 셀렉터 보정 (★ 불가건 발생 시 테스트) — 공매는 검증완료
1차 버전은 화면 추정으로 작성됨. **공매 경로는 06-02 검증 완료**. 경매(법원경매) 불가건은 실제 샘플이 떠야 검증 가능 → 아래 #1 미검증과 동일 트리거. 실제 옥션 로그인해서 돌려보고 보정:
- `scan_address(text)` — 상세페이지에서 **소재지(주소)** 추출 (find_court 입력용). 현재 "소재지|주소" 라벨 또는 시도 시작 줄 정규식. 실제 텍스트 보고 보정.
- `parse_jongryo(detail_text)` — "본사건은 ○○(으)로 경매절차가 종결되었습니다" 정규식. 실제 문구 확인.
- `open_detail_text` — `auction/ca_view.php?product_id=` / `pubauct/view.php?product_id=` 가 맞는 상세 URL인지, body 텍스트에 주소/종결문구/기일 다 있는지.
- `date_hit` (입찰일자 일치) — 상세에 매각기일 표기 포맷 확인.
- 디버그: 실행 로그의 `[경매 i/N]`, `→ ✅/⚠키불일치` 라인 + 안 잡힌 필드 보고 받아 보정.

### ✅ #3 03.k.py(건별) 연도 검색 — 완료 (2026-06-02)
- `extract_auction_parts` year_val 빈 케이스 → num1 '전체연도'(value=0) 강제 fallback 추가. 기존 skip→연도필터 누락 방지. (디버그HTML로 전체옵션 value=0 확인)

### #4 (낮음) P2e — stale 참조 — 일부 완료 (2026-06-02)
- ✅ 텔레그램 흐름모달 라벨: `_MSG_FLOW` seq10 '변경'→'불가', statusMap `item_card.status` '취소'→'불가' (표시라벨, @1270).
- ⬜ **남음**: 월별집계 `sumTotals.취소/변경`(js-app ~13548) — **GAS 백엔드 summary 키와 엮임**. 프론트만 바꾸면 카운트 0 → 백엔드(분류 CATEGORIES) 같이 봐야 함. 안 깨지므로 낮음.

### ✅ #5 불가/폐기 사유 탭 — 결정: 항상 표시(현행 유지)
- 비활성화(흐리게) 하지 않고 **일단 다 보여줌**. 사용자 테스트 완료 후 재안내 예정. (2026-06-02 결정)

---

## 주요 파일/위치
- `00.imageup/04.cc.py` — **진행사항 확인 크롤러** (3필드 파싱/연도검색/3키검증/종결문구/공매경로). ※ 구 03.cc.py는 shim
- `00.imageup/03.k.py` — 건별 이미지생성 크롤러 (연도 num1 선택 포함)
- `00.imageup/court_jurisdiction.py` — 주소→MAPS법원명 (`find_court`, `_JURISDICTION_MAP`)
- `00.auction1/mj_tabs.js` — 불가확인 탭 UI(`renderCcResults`/`sendCcToMaps`, CAP_SPEC.cc), `/api/maps-changecancel`
- `00.auction1/index.html` — 불가확인 카드(cc), 입력 textarea(`data-role="cases"`)
- `00.auction1/crawler.py` — `/api/maps-gas`·`/api/maps-changecancel` 프록시
- `js-app.html` — `handleDownload7DaysList`(3필드 다운로드)
- `SheetDB.js` — `uploadChangeCancel`(3키→불가/Z/AA), `saveItemReason`, `ensureItemReasonColumns_`, `readAllData`(Z/AA 읽기)

## 배포/테스트 규칙
- GAS(js-app/SheetDB/index 등): `clasp push -f` → `clasp deploy -i AKfycby1SnLYJmPQ9PU0JlEZC5rG3e9y9s6wMVrsPeG_gqgDBnK9FMkyVPb3v5V0DFI14ETZiA -d "..."` (동일 deployment, URL 불변)
- 매니저(00.auction1/*, 00.imageup/04.cc.py, 03.k.py): 로컬 — clasp 배포 X, git 커밋만. .py는 매 실행 새로 spawn → 매니저 재시작 불필요(새로고침/재실행만).
- HTML 내 JS 문법검증: `node -e` 로 `<script>` 블록 vm.Script 체크. SheetDB/mj_tabs: `node --check`. 04.cc.py/03.k.py: `python -m py_compile`.
- 04.cc.py 직접 검증법: cc 활성계정(imageup_config.json `cc.accounts` enabled) + `MJ_IMAGEUP_ACCOUNTS_JSON`/`MJ_IMAGEUP_CASES_JSON`/`MJ_IMAGEUP_HEADLESS=1` env 주입 후 실행. 매니저 env: PYTHONIOENCODING=utf-8.
