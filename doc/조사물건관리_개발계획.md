# 조사물건 관리 - 개발 계획서
작성일: 2026-03-31

---

## 1. 전체 개발 범위

### 1-1. 조사물건 관리 메뉴 신규 생성
- 위치: 사이드바 → 입찰물건관리 아래
- 기능: 옥션 크롤링 데이터 조회, 조사자 텔레그램 전송

### 1-2. Python 크롤링 스크립트
- 파일명: `00.imageup/아파트, 84, 2-10억.py`
- 로그인: mjjang1 (기존 01.i.py 패턴 참조)
- 검색조건: 아파트 / 84m² / 2~10억 / 개찰횟수 2~3
- 결과: 중복 제외 후 MAPS API로 전송

### 1-3. 회원관리 - 구분 다중선택
- `조사자` 구분값 추가
- 구분 필드를 다중선택(체크박스) 방식으로 변경

### 1-4. 텔레그램 조사요청
- 메시지: "조사요청 물건이 있습니다. 확인 하시겠습니까? (예/아니오)"
- 예 → MAPS 상태 `확인`으로 업데이트

---

## 2. DB 설계 - search 시트 (19컬럼)

| # | 필드명 | 설명 | 비고 |
|---|--------|------|------|
| 1 | search_id | 조사물건 ID (PK) | S+yyMMddHHmmss+3자리난수 |
| 2 | in-date | 입찰일자 | yyMMdd |
| 3 | sakun_no | 사건번호 | |
| 4 | court | 법원 | |
| 5 | item_type | 부동산타입 | 크롤링값 |
| 6 | item_summary | 물건요약 | 크롤링값 |
| 7 | item_area | 물건면적 | 크롤링값 |
| 8 | item_status | 물건상태 | 크롤링값 |
| 9 | kamjungka | 감정가 | 크롤링값 |
| 10 | min_bid_price | 최저입찰가 | 크롤링값 |
| 11 | min_bid_rate | 최저입찰율 | 크롤링값 |
| 12 | address | 주소 | 크롤링값 |
| 13 | note1 | 비고1 | |
| 14 | note2 | 비고2 | |
| 15 | josaja | 조사자 | members.member_name |
| 16 | reg_date | 등록일시 | yyMMdd HHmmss |
| 17 | reg_member | 등록자 | system / web |
| 18 | auction_id | 옥션 물건ID | |
| 19 | josa_status | 조사상태 | 신규/요청/확인/완료/기타 |

**중복체크**: sakun_no + in-date 조합

---

## 3. 파일별 변경 내역

### 3-1. Code.js ✅완료
- `GUBUN_OPTIONS`에 `'조사자'` 추가
- `SEARCH_SHEET_NAME = 'search'` 상수 추가

### 3-2. SheetDB.js ✅완료
추가된 함수:
- `initSearchSheet_()` — search 시트 헤더 초기화
- `readAllSearchItems()` — 전체 조회
- `saveSearchItem(data)` — 단건 저장 (중복체크 포함)
- `saveSearchItemsBatch(items)` — 일괄 저장
- `updateSearchItemStatus(searchId, josaStatus, josaja)` — 상태 업데이트
- `getInvestigators()` — gubun에 '조사자' 포함한 회원 목록
- `handleSearchApiPost_(payload)` — 외부 API 핸들러

### 3-3. Main.js ✅완료
- `doGet`: `?admin=KEY&api=getSearchItems` → JSON 반환
- `doPost`: `payload.api_action` 존재 시 → JSON API 처리 (Telegram 웹훅과 분리)

### 3-4. index.html ⬜미완료
변경 위치: `<nav id="mainSidebar">` 내부
```html
<!-- 입찰물건관리 버튼 다음에 추가 -->
<button id="menu-SearchManagement" onclick="navigateTo('SearchManagement')"
    class="py-4 px-6 text-base text-left text-gray-700 hover:bg-gray-100 transition duration-150">조사물건
    관리</button>
```

추가할 섹션: `SearchManagement` div (리스트 뷰)
- 검색필터: 입찰일자, 사건번호, 법원, 조사상태, 조사자
- 테이블 컬럼: 입찰일자 / 사건번호 / 법원 / 물건요약(주소) / 감정가 / 최저입찰가 / 물건면적 / 조사상태 / 조사자 / 전송
- 상단 버튼: 새로고침, 조사요청 전송(선택)

회원관리 구분 필드: 다중선택 체크박스 → `gubun` 콤마 구분 저장

### 3-5. js-app.html ⬜미완료
추가 내용:
- `TAB_LAYOUTS['SearchManagement'] = 'flex'`
- `navigateTo` 핸들러에 `SearchManagement` 분기 추가
- `allSearchData` 전역 변수
- `loadSearchData()` — `readAllSearchItems` GAS 호출
- `renderSearchList()` — 테이블 렌더링
- `applySearchFilters()` — 필터 적용
- `sendJosaRequestTelegram(searchId)` — 단건 텔레그램 전송
- `handleBulkJosaRequest()` — 선택 일괄 전송
- 조사자 드롭다운 동적 로딩 (`getInvestigators`)

### 3-6. TelegramService.js ⬜미완료
추가 내용:
- `sendJosaRequestTelegram_(searchId, chatId)` — 조사요청 메시지 전송
  - 메시지: "조사요청 물건이 있습니다.\n[사건번호] [법원] [입찰일자]\n확인 하시겠습니까?"
  - 인라인 버튼: `[예] [아니오]`
  - callback_data: `MJ|JOSA_YES|{searchId}` / `MJ|JOSA_NO|{searchId}`
- `handleTelegramWebhook_` 내 callback_query에 `JOSA_YES` / `JOSA_NO` 처리 추가
  - `JOSA_YES` → `updateSearchItemStatus(searchId, '확인')`
  - `JOSA_NO` → `updateSearchItemStatus(searchId, '요청취소')`

### 3-7. 00.imageup/아파트, 84, 2-10억.py ⬜미완료
구조:
```
1. 로그인 (mjjang1 / 28471298)
2. 경매 검색 페이지 이동
3. 검색조건 세팅:
   - s_class (부동산타입): 아파트
   - min_price: 20000 (2억)
   - max_price: 100000 (10억)
   - b_area1: 84 (전용면적)
   - b_count1: 2, b_count2: 3 (개찰횟수)
4. 검색 실행
5. 결과 파싱 (각 아이템별):
   - in-date: td:nth-child(6) 텍스트에서 날짜 추출
   - sakun_no: DOM 추출 (extract_sakun_from_dom 참조)
   - court: get_court_from_text() 함수
   - item_type, kamjungka, min_bid_price, min_bid_rate, item_status: JS로 동적 추출
   - address, item_area: CSS 셀렉터로 추출
   - item_summary: section div 추출
   - auction_id: product_id 추출 (01.i.py product_id 패턴 참조)
6. 기존 MAPS 데이터 조회 (GET ?admin=KEY&api=getSearchItems) → 중복 sakun_no 제외
7. 신규 아이템 POST (api_action=saveSearchItems) → MAPS 등록
```

API 연동:
- GAS 웹앱 URL: `https://script.google.com/macros/s/AKfycby1SnLYJmPQ9PU0JlEZC5rG3e9y9s6wMVrsPeG_gqgDBnK9FMkyVPb3v5V0DFI14ETZiA/exec`
- ADMIN_KEY: 실행 전 `getAdminSecretKey_()` 결과값 환경변수에 설정
- Headers: `Content-Type: application/json`

---

## 4. 조사 워크플로우

```
크롤링 등록(신규)
    ↓
관리자가 조사자 배정 → josa_status=요청
    ↓
텔레그램 전송 → "조사요청 물건이 있습니다. 확인하시겠습니까?"
    ↓
조사자 [예] 클릭 → josa_status=확인
조사자 [아니오] 클릭 → josa_status=요청취소
    ↓
(완료 처리) → josa_status=완료
```

---

## 5. 주요 참고사항

- `gubun` 다중선택: 콤마 구분 문자열로 저장 (예: `"회원,조사자"`)
- `getInvestigators()`: `gubun.split(',').includes('조사자')` 로 필터
- 텔레그램 전송 전에 조사자의 `telegram_chat_id` 필수 확인
- search 시트는 최초 접근 시 `initSearchSheet_()` 자동 호출됨

---

## 6. 진행 상태

| 파일 | 상태 |
|------|------|
| Code.js | ✅ 완료 |
| SheetDB.js | ✅ 완료 |
| Main.js | ✅ 완료 |
| index.html | ✅ 완료 |
| js-app.html | ✅ 완료 |
| TelegramService.js | ✅ 완료 |
| 아파트, 84, 2-10억.py | ✅ 완료 |
| GAS 푸쉬/배포 | ⬜ 사용자 승인 후 진행 |

---

## 7. 이어서 개발 시 참고

### Python 스크립트 실행 전 필수 설정
`00.imageup/아파트, 84, 2-10억.py` 파일 상단 `ADMIN_KEY` 변수에
MAPS GAS 에디터 → Admin 메뉴 → 관리자 KEY 확인 값 입력 필요

### MAPS UI 확인 사항
- `조사물건 관리` 메뉴 → 사이드바에 `입찰물건관리` 아래에 위치
- 조사자 배정 → 리스트 내 셀렉트박스로 바로 변경 가능
- 전송 버튼(✈) → 조사자 미배정 시 에러 메시지 표시
- 선택 일괄 전송 → 상단 "선택 조사요청 전송" 버튼

### 텔레그램 콜백 키워드
- `MJ|JOSA_YES|{search_id}` → 상태 `확인` 업데이트
- `MJ|JOSA_NO|{search_id}` → 상태 `요청취소` 업데이트

### 회원관리 구분 변경
- `gubun` 필드: 콤마 구분 다중값 저장 (`"회원,조사자"` 형태)
- `getInvestigators()`: gubun에 '조사자' 포함한 회원 반환
