# 0529 작업 — 변경/취소 워크플로 (이어서 진행할 때 이 파일 참고)

> **재개 트리거**: 사용자가 "**0529 작업 내용 진행해줘**" 라고 하면 이 파일 다시 읽고 [다음 진행 항목](#다음-진행-항목-사용자-결정-우선순위) 의 #5 부터 순서대로 진행.

---

## 1. 0528 (오늘) 작업 흐름

`286a4a4` 의 대규모 변경(@1231) 이 GAS 인증 다이얼로그에서 SyntaxError 발생 → **빈 화면 사고**.
→ revert 후 **호환 모드 (기존 '취소' 유지 + 새 enum 추가)** 로 작게 끊어서 phase 별 재진행.

| 배포 | 커밋 | 내용 |
|---|---|---|
| @1231 | `676a4a4` | 변경/취소 워크플로 + 폐기 rename 대규모 ❌ **빈화면** |
| @1232 | `57501f8` | revert → @1230 복귀 |
| @1233 | `4ad3e11` | 상태 `<option>` 4곳 × 2개 (폐기, 변경/취소) 추가 |
| @1234 | `970ccc6` | 상태 검색 체크박스 (search/cal/d1) 에 폐기/변경/취소 추가 |
| @1235 | `2ecd3a2` | `변경/취소` → `변경` rename |
| @1236 | `0d6ae92` | CSS 색상 (폐기=#dc2626, 변경/취소=#111827, font-weight:600) |
| @1237 | `8910c27` | GAS 분류 로직 — `SheetDB.js:4644` + `:6082-6085` 에 폐기/변경 인식 |
| @1238 | `3cc49db` | 대시보드 "물건상태 관리" 카드에 폐기/변경 컬럼 추가 |
| (참고) @1239 | `c51db37` | 별도 세션: 회차 추가 신규수업 버그 fix |

---

## 2. 사용자가 직접 할 일 (오늘 미완)

✅ **items 시트의 `stu_member` 컬럼: `취소` → `폐기` 일괄 변경**
- 시트에서 E열 선택 → Ctrl+H → 찾기 `취소` / 바꾸기 `폐기` / "전체 일치" 체크 → 모두 바꾸기
- telegram_requests 시트(이력) 는 **그대로 두기 권장** (코드가 양쪽 enum 모두 인식)

---

## 3. 결정된 enum 체계

| 시트 값 | 의미 | 표시 색상 |
|---|---|---|
| `취소` | 옥션원 취소 (외부 사유, 미래 자동 등록용) | 검정 (#111827) |
| `변경` | 옥션원 변경 (외부 사유) — **신규** | 검정 (#111827) |
| `폐기` | 내부 결정 (입찰 안 가기로 결정) — **신규** | 빨강 (#dc2626) |
| 기존 상품/검증/미정/추천/입찰 | 기존 그대로 | 기존 |

---

## 4. 다음 진행 항목 (사용자 결정/우선순위)

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| 1 | **통계 분류 정책** — `cancel_approved_web` (입찰→폐기/변경/취소), `bid_pending_web` (추천→폐기/변경/취소) 매핑 정책 | 🟡 **사용자 결정 후 진행** ("나중에 고민해서 잡자") | SheetDB.js:4614-4623 |
| 2 | **lockedStatus 배열** 에 새 enum — 수업회차/d1 에서 폐기/변경/취소 도 잠금 처리 | 🟢 진행 가능 | preview.html:18883/19088, js-app.html 동일 |
| 3 | **입찰 목록 테이블 cell 색상** — JS case 문 (stu-tag) 에 폐기/변경 추가 | 🟢 진행 가능 | preview.html:10491 등 case 문 |
| 4 | **입찰일정 상단 버튼 그룹** — `btn-top-chg` 옆 폐기/변경 버튼 (CSS 별도 색상) | 🟢 진행 가능 | index.html:818 / preview.html:4490 |
| 5 | **이미지등록요청 안내 메시지 경로** — `C:\LJW\MAPS_TEST\...` → `C:\LJW\01. SYSTEM\MAPS_TEST\...` 갱신 (@1231 에서 했으나 revert) | 🔴 **사용자 명시 요청 — 우선 처리** | preview.html:17642 (1줄) |
| 6 | **매니저 새 서브탭 "변경/취소 확인" + 옥션원 자동 조회 + MAPS 자동 전송** | ✅ **완료 (@1243, 2026-05-29)** | 매니저쪽만 분리 재적용(캡처/파일복사삭제/리사이저 제외). 03.cc.py + mj_extensions cc + mj_tabs cc 탭 + crawler 프록시 + SheetDB.js uploadChangeCancel(현행 분리 enum 취소/변경/폐기에 맞춤). 커밋 a6a3d58 |
| 7 | **stash 된 미적용 변경** — `mj_extensions.py` 의 cc 계정 fallback | ✅ **완료 (@1243)** — get_accounts cc→k/i/d fallback 적용됨. stash@{0} 은 이제 불필요(drop 가능) | ~~`WIP-cc-account-fallback-before-revert`~~ |

→ **#5 부터 시작 권장** (가장 짧고 명확). 그 다음 #2 → #3 → #4 순서.

---

## 5. 코드 주요 위치 (재개 시 빠르게 찾을 곳)

- **GAS 분류 로직** (이미 처리): [SheetDB.js:4644](../SheetDB.js#L4644), [SheetDB.js:6082-6085](../SheetDB.js#L6082)
- **대시보드 카드** (이미 처리): [preview.html:4302](../preview.html#L4302), [index.html:624](../index.html#L624), [preview.html:13075](../preview.html#L13075), [js-app.html:4214](../js-app.html#L4214)
- **CSS 색상** (이미 처리): [css.html:3716 직전](../css.html#L3716), [preview.html:3996 직전](../preview.html#L3996)
- **목업** (참고용): [doc/변경취소_목업.html](변경취소_목업.html) — @1231 revert 시 함께 사라짐, 필요 시 git show 676a4a4 으로 복원 가능

---

## 6. 직전 사고 (@1231 빈화면) 교훈

- **GAS 새 server function 추가** = 권한 변경 트리거 → 인증 다이얼로그 syntax error 가능성
- 호환 모드(추가만)·분기 조건 확장만 = 안전. **rename / 새 함수 = 위험**
- 큰 변경 한 번에 ❌. **phase 작게 끊고 배포 후 확인** ✓
