/**
 * === 배포 정보 (GitHub Actions 자동 업데이트) ===
 * 이 파일은 GitHub Actions에서 clasp push 전에 자동으로 업데이트됩니다.
 * GAS에서 이 파일을 열면 마지막 배포 시간을 확인할 수 있습니다.
 */
var DEPLOY_INFO = {
    version: 'local',
    commit: 'local',
    deployedAt: 'not deployed via GitHub Actions',
    branch: 'local'
};
